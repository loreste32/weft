fn producer(ch) {
    send(ch, 1)
    send(ch, 2)
    close(ch)
}


fn main -> Result[any] {
    ch := channel(2)
    spawn(producer, ch)
    a := recv(ch)?
    b := recv(ch)?
    say(a, b)
    g := group()
    g.go(fn() { 10 })
    g.go(fn() { 20 })
    rs := g.wait()?
    say(rs)
}

