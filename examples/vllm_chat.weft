fn main -> Result[any] {
    log.info("vllm base", vllm.base())
    vllm.health()?
    models := vllm.list()?
    say("models:", models)
    reply := vllm.chat({
        "prompt": "Give three bullet uses for a local LLM server.",
        "system": "Be concise.",
    })?
    say("reply:", reply)
}

