fn square(x) { x * x }


fn main {
    say(map([1, 2, 3, 4], square))
    say(map([1, 2, 3, 4], square, 2))
    xs := [1, 2, 3, 4, 5, 6]
    evens := filter(xs, fn(n) { n % 2 == 0 })
    say(evens)
    mut built := []
    for i in range(5) { push(built, i) }
    say(built)
}

